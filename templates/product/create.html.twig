{% extends "base.html.twig" %}


{% block title %}
	Création d'un produit
{% endblock %}

{% block body %}
	<h1>Nouveau produit</h1>

	{{form_start(formView)}}
	{{form_errors(formView)}}

	<div class="row">
		<div class="col">
			{{form_row(formView.name)}}
			{{form_row(formView.price)}}
		</div>
		<div class="col">
			{{form_row(formView.mainPicture)}}
			{{form_row(formView.category)}}
			{{form_row(formView.shortDescription)}}
		</div>
	</div>


	<button type="submit" class="btn btn-primary">
		<em class="fas fa-save"></em>
		Créer le produit
	</button>
	{{form_end(formView)}}

{% endblock %}

{# {% block form_row %}
Ici on crée notre propre theme, pas utile quand on prend le theme sur symfony
	localement dans ce bloc, la variable form va représenter ce qu'on a envoyé au dessus (formView.name...) 
	<div class="form-group">
		{{form_label(form)}}
		{{form_widget(form, {'attr' : {'class' : 'form-control'}}
		)}}
		{{form_help(form)}}
		{{form_errors(form)}}
	</div>
{% endblock %} #}
